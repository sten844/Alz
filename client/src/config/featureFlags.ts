// New sections are intentionally kept inactive until the site owner explicitly approves publication.
// Keeping these flags false allows normal site updates without exposing unfinished features.
export const PENDING_FEATURES = {
  lifestyleSection: false,
  redCategoryButtons: false,
  secondaryArticleSeries: false,
} as const;
